#include <stdio.h>

int main()
{
    int x;
    scanf("%d", &x);

    printf("X++ : %d\n", x++);
    printf("++X : %d\n", ++x);
    printf("X-- : %d\n", x--);
    printf("--X : %d\n", --x);

    // Run this and find out the values.
    // The ones given in the sample output are wrong

    return 0;
}

#include <stdio.h>

int main()
{

    int a;                  //  a)  Declare a variable uninitialized
    int b = 5;              //  b)  Declare and  initialize a variable in one statement
    int c = 10, d = 25;     //  c)  Declare and initialize multiple variables with different values in one statement
    int e = 100, f = 100;   //  d)  Declare and initialize multiple variables with the same value in one statement

    // for (c) and (d) you can declare and initialize more variables if you want using a comma to seperate them

    return 0;
}
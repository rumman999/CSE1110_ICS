#include <stdio.h>
#include <stdbool.h> // Required for bool data types

int main()
{
    
    double a;
    bool b;

    a = 3.14;
    b = true;

    printf("The double value: %e\n", a);
    printf("The double value: %d", b);

    return 0;
}